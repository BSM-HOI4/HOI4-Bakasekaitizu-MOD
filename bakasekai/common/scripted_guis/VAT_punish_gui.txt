scripted_gui = {

	VAT_punish_gui = {
		window_name = "VAT_punish_gui"
		context_type = player_context

		visible = {
			# FIX(errorlog): window "VAT_punish_gui" の .gui 定義が未作成のため、
			# tag=VAT で表示しようとすると "Undefined GUI_TYPE" でクラッシュ恐れ。
			# 窓を実装するまで無効化。実装時に always=no を tag=VAT へ戻すこと。
			always = no
			#tag = VAT
		}

	}
}

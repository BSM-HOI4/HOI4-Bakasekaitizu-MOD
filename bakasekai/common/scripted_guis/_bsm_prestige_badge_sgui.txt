# 威信ランキング バッジ(国旗左上の丸に世界順位を表示、tooltipで階級/威信実数値)
# player_context の表示専用GUI。bsm_prestige_position / _rank / bsm_prestige は
# 起動時(bsm_econ_startup)と月次で更新される。

scripted_gui = {
	bsm_prestige_badge = {
		context_type = player_context
		window_name = "bsm_prestige_badge_window"

		visible = {
			has_variable = bsm_prestige_rank
		}

		ai_enabled = { always = no }
	}
}

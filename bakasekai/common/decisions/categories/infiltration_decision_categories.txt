communist_infiltration = {
	allowed = {
		OR = {
			original_tag = JPN
			original_tag = MEN
			original_tag = MME
		}
	}

	visible = {
		is_subject = no
		if = {
			limit = { original_tag = JPN }
			OR = {
				MME = { is_subject_of = ROOT }
				MEN = { is_subject_of = ROOT }
			}
		}
		NOT = { has_government = communism_ideology }
		any_other_country = {
			has_completed_focus = PRC_anti_japanese_expedition
		}
	}
}
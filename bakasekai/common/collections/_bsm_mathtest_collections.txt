# =====================================================================
# 数式テスター用コレクション (集約検証 every_collection の被験体)
# tracked な collections.txt を汚さないよう、専用の独立ファイルにする。
# every_collection(数式集約)が本modで動くかを ACC で実測するためだけの最小コレクション。
# =====================================================================

bsm_mt_all_countries = {
	input = game:all_countries
	operators = {
		limit = { exists = yes }
	}
	name = COLLECTION_BSM_MT_ALL_COUNTRIES
}

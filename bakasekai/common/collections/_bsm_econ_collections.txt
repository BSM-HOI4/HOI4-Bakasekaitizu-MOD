# =====================================================================
# 経済系システム用コレクション (共有 collections.txt を汚さない独立ファイル)
# bsm_world_countries: 威信(System C)の「世界からの敬意」= Σ opinion@ROOT 集約に使う全存在国。
# =====================================================================

bsm_world_countries = {
	input = game:all_countries
	operators = {
		limit = { exists = yes }
	}
	name = COLLECTION_BSM_WORLD_COUNTRIES
}

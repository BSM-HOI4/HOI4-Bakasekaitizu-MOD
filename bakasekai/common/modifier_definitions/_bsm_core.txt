weekly_Cultural_Degree = {
	color_type = good
	value_type = percentage
	precision = 2
}

base_Cultural_Degree = {
	color_type = good
	value_type = percentage
	precision = 0
}

daily_National_Unity_Power_gain = {
	color_type = good
	value_type = number
	precision = 0
}

National_Unity_Power_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

Unified_Currency_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

Unified_Currency_war_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

Unified_Currency_religion_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

#経済同盟内での影響力補正（議席配分・UC/PPバフ配分の重み付けに使用）
ea_influence_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

bsm_consumer_goods_factories_per_million_population = {
	color_type = bad
	value_type = number
	precision = 2
}

bsm_consumer_goods_factories_per_100_army_battalions = {
	color_type = bad
	value_type = number
	precision = 2
}

bsm_political_power_gain_per_stability = {
	color_type = good
	value_type = number
	precision = 2
}

bsm_command_power_gain_per_war_support = {
	color_type = good
	value_type = number
	precision = 2
}

bsm_political_power_cost_per_owned_state = {
	color_type = bad
	value_type = number
	precision = 2
}

bsm_population_growth_factor_per_living_standard = {
	color_type = good
	value_type = percentage
	precision = 2
}

bsm_domestic_strike_chance = {
	color_type = bad
	value_type = percentage
	precision = 2
}

bsm_domestic_riot_chance = {
	color_type = bad
	value_type = percentage
	precision = 2
}

bsm_monthly_worker_death_chance = {
	color_type = bad
	value_type = percentage
	precision = 2
}


#鉱山の収入補正値
MD_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

#アノマリー解析速度補正
AS_AN_analyze_speed_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

naval_repair_support_value_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

naval_general_support_value_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

NC_exchange_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

# 威信(System C)を +/- するカスタム modifier。idea・focus・event が付与し、
# bsm_prestige_compute が modifier@bsm_prestige_flat / modifier@bsm_prestige_factor で読む。
# Vic3の「national tier 基礎値(=flat)」「technologies +5〜10%(=factor)」に相当。

bsm_prestige_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

bsm_prestige_flat = {
	color_type = good
	value_type = number
	precision = 0
}

# GDP を +/-% するカスタム modifier。法律(金融法/労働法)・idea・event が付与し、
# bsm_econ_update_gdp が modifier@bsm_gdp_factor で読み、実質GDPに (1 + factor) を乗算する。
bsm_gdp_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

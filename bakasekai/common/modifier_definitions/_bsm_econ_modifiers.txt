# 威信(System C)を +/- するカスタム modifier。idea・focus・event が付与し、
# bsm_prestige_compute が modifier@bsm_prestige_flat / modifier@bsm_prestige_factor で読む。
# Vic3の「national tier 基礎値(=flat)」「technologies +5〜10%(=factor)」に相当。

bsm_prestige_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}

bsm_prestige_flat = {
	color_type = good
	value_type = number
	precision = 0
}

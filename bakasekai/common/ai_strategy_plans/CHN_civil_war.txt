CHN_civil_war_plan = {
    name = "Chinese Civil War AI Plan"
    desc = "AI plan for the Chinese Civil War."

    allowed = {
        OR = {
            tag = CHI
            tag = CHN
            tag = SHX
            tag = XSM
            tag = MME
            tag = GXC
            tag = SIK
        }
    }

    enable = {
        has_global_flag = CHN_civil_war
    }

    abort = {
        NOT = { has_global_flag = CHN_civil_war }
    }

    # 優先する国家方針（例: CHI_focus_1 など、必要に応じて追加・修正してください）
    ai_national_focuses = {
        # CHI_focus_1 CHI_focus_2
    }

    # 国家方針の優先度補正（例: CHI_focus_1 = 3 など、必要に応じて追加・修正してください）
    focus_factors = {
        # CHI_focus_1 = 3
    }

    # 研究カテゴリの優先度補正（例: infantry = 2 など、必要に応じて追加・修正してください）
    research = {
        infantry = 3
        support = 2
        artillery = 2
        # 他カテゴリは必要に応じて追加
    }

    traits = {
        captain_of_industry = 3
        # 他のtraitsは必要に応じて追加
    }

    # 必要に応じてideas, traits, ai_strategy, weight等も追加可能
}

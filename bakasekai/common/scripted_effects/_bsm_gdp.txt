# BSMS_GDP_household_budgets = {
# 	set_variable = { dif = difficulty }
# 	add_to_variable = { dif = 1 }
# 	if = {
# 		limit = {
# 			has_government = democratic_ideology
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.9 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}	
# 	else_if = {
# 		limit = {
# 			has_government = communism_ideology
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.25 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = fascism_ideology
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.55 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = neutrality_ideology
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.6 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = civilism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.95 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = conservative_democracy
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.85 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = constitutional_monarchy
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.8 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = direct_democracy
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.78 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = futurism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.56 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = intellectualism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.45 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = mythologicalism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.2 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = rightneutrality
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.35 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = anarchism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.01 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = stupidism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.65 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = technicalism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.65 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = philanthropy
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 1.25 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = transformationism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.1 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = ruinism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.01 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = longitudinalism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.7 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = horizontalism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.7 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = hinnulism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.7 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# 	else_if = {
# 		limit = {
# 			has_government = kyonulism
# 		}
# 		set_variable = { BSMS_GDP_household_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_household_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		set_temp_variable = { temp = modifier@consumer_goods_factor }
# 		multiply_variable = { cf = temp }#mpp = 人的x民需
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.7 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_household_budget = mpp }

# 	}
# }
# BSMS_GDP_companyhold_budgets = {

# 		set_variable = { BSMS_GDP_companyhold_budget = stability } ##家計=安定度
# 		multiply_variable = { BSMS_GDP_companyhold_budget = num_occupied_states }##家計=家計x総州
# 		set_variable = { cf = num_of_available_civilian_factories  }#cf = 民需
# 		set_variable = { mpp = manpower_k }#mpp = 人的
# 		multiply_variable = { mpp = cf }#mpp = 人的x民需
# 		multiply_variable = { mpp = core_compliance }#mpp = mppx迎合度平均
# 		multiply_variable = { mpp = 0.25 }#mpp = mppx迎合度平均
# 		divide_variable = {	mpp = threat }
# 		divide_variable = {	mpp = dif }
# 		multiply_variable = { BSMS_GDP_companyhold_budget = mpp }

# }

# BSMS_GDP_sum = {
# 	set_temp_variable = { temp = ROOT.BSMS_GDP_household_budget }
# 	set_temp_variable = { temp1 = ROOT.BSMS_GDP_companyhold_budget }
# 	add_to_variable = { temp = temp1 }
# 	multiply_variable = { temp = 0.5 }
# 	set_variable = {
# 		BSMS_GDP = temp
# 	}
# }
# Economic System Dynamic Modifiers

# Penalty for Low Currency
# Applied when Unified Currency < Required EC
ucs_low_currency_penalty_modifier = {
	icon = gfx/interface/icons/modifiers/stability_icon.dds # Placeholder icon
	stability_factor = -0.10 # -10% Stability
	# You can add more effects here
}

# Penalty for Over State Limit
# Applied when Owned States > State Limit
ucs_over_state_limit_penalty_modifier = {
	icon = gfx/interface/icons/modifiers/political_power_icon.dds # Placeholder icon
	political_power_gain = -0.20 # -20% PP Gain
	stability_factor = -0.05 # -5% Stability
}

# Bonus from Unified Economic Power (Economic Alliance)
# Variable: unified_economic_power_bonus
# This modifier should be scaled by the variable.
# Example: 100 Power -> +10% Construction Speed?
ucs_economic_power_bonus_modifier = {
	icon = gfx/interface/icons/modifiers/production_speed_icon.dds # Placeholder icon
	# Effects scaled by the variable 'unified_economic_power_bonus'
	# Assuming the variable is set to e.g. 0.1 for 10%

	# Note: Dynamic modifiers in HOI4 usually take a variable as an argument when applied,
	# or they just enable static effects.
	# For variable scaling, we usually use 'modifier_rule' or just apply the variable directly in the effect?
	# Actually, dynamic modifiers in HOI4 (since NSB/BBA) can use 'value' to scale.

	# Let's assume we use 'enable' triggers for static tiers, OR we use the new variable system if supported.
	# But standard dynamic modifiers just enable a set of modifiers.
	# To have SCALING modifiers, we often use 'targeted_modifier' or just 'political_power_gain = var:my_var'.

	# Let's try using variable directly in the modifier.
	# production_speed_industrial_complex_factor = var:unified_economic_power_bonus
	# consumer_goods_factor = var:unified_economic_power_bonus_neg # (if we want reduction)

	# For now, let's just use a placeholder effect that we can tune.
	# We will set 'unified_economic_power_bonus' to the value we want (e.g. 0.05 for 5%).

	industrial_capacity_factory = var:unified_economic_power_bonus
	research_speed_factor = var:unified_economic_power_bonus
}

# Featured major powers — opening-phase smoke tests.
# Tags are the countries highlighted in the 1936 "Gathering Storm" bookmark
# (common/bookmarks/the_gathering_storm.txt). Each major must spawn and survive
# the opening months. Native HOI4 scripted test bundle; see _documentation.info.

last_date = "1936.7.1"

tests = {
	BTL_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { BTL = { exists = yes } }
		fail = {
			date > "1936.2.1"
			BTL = { exists = no }
		}
		run_count = 1
	}

	BTL_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { BTL = { has_capitulated = yes } }
		run_count = 1
	}

	MYS_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { MYS = { exists = yes } }
		fail = {
			date > "1936.2.1"
			MYS = { exists = no }
		}
		run_count = 1
	}

	MYS_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { MYS = { has_capitulated = yes } }
		run_count = 1
	}

	JPN_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { JPN = { exists = yes } }
		fail = {
			date > "1936.2.1"
			JPN = { exists = no }
		}
		run_count = 1
	}

	JPN_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { JPN = { has_capitulated = yes } }
		run_count = 1
	}

	MAC_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { MAC = { exists = yes } }
		fail = {
			date > "1936.2.1"
			MAC = { exists = no }
		}
		run_count = 1
	}

	MAC_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { MAC = { has_capitulated = yes } }
		run_count = 1
	}

	WES_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { WES = { exists = yes } }
		fail = {
			date > "1936.2.1"
			WES = { exists = no }
		}
		run_count = 1
	}

	WES_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { WES = { has_capitulated = yes } }
		run_count = 1
	}

	TEX_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { TEX = { exists = yes } }
		fail = {
			date > "1936.2.1"
			TEX = { exists = no }
		}
		run_count = 1
	}

	TEX_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { TEX = { has_capitulated = yes } }
		run_count = 1
	}

	MEX_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { MEX = { exists = yes } }
		fail = {
			date > "1936.2.1"
			MEX = { exists = no }
		}
		run_count = 1
	}

	MEX_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { MEX = { has_capitulated = yes } }
		run_count = 1
	}

	CHL_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { CHL = { exists = yes } }
		fail = {
			date > "1936.2.1"
			CHL = { exists = no }
		}
		run_count = 1
	}

	CHL_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { CHL = { has_capitulated = yes } }
		run_count = 1
	}

	CLF_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { CLF = { exists = yes } }
		fail = {
			date > "1936.2.1"
			CLF = { exists = no }
		}
		run_count = 1
	}

	CLF_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { CLF = { has_capitulated = yes } }
		run_count = 1
	}

	DIS_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { DIS = { exists = yes } }
		fail = {
			date > "1936.2.1"
			DIS = { exists = no }
		}
		run_count = 1
	}

	DIS_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { DIS = { has_capitulated = yes } }
		run_count = 1
	}

	HLW_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { HLW = { exists = yes } }
		fail = {
			date > "1936.2.1"
			HLW = { exists = no }
		}
		run_count = 1
	}

	HLW_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { HLW = { has_capitulated = yes } }
		run_count = 1
	}

	LVE_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { LVE = { exists = yes } }
		fail = {
			date > "1936.2.1"
			LVE = { exists = no }
		}
		run_count = 1
	}

	LVE_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { LVE = { has_capitulated = yes } }
		run_count = 1
	}

	NEV_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { NEV = { exists = yes } }
		fail = {
			date > "1936.2.1"
			NEV = { exists = no }
		}
		run_count = 1
	}

	NEV_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { NEV = { has_capitulated = yes } }
		run_count = 1
	}

	NYY_exists_at_start = {
		# A featured major must actually spawn. Fails only if, a month in, the
		# country still does not exist (failed to be created at game start).
		success = { NYY = { exists = yes } }
		fail = {
			date > "1936.2.1"
			NYY = { exists = no }
		}
		run_count = 1
	}

	NYY_survives_opening = {
		# Featured major should not capitulate in the opening months.
		success = { date > "1936.6.1" }
		fail = { NYY = { has_capitulated = yes } }
		run_count = 1
	}
}

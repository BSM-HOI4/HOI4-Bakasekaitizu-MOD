# Harness self-check: confirms the mod scenario loads and the scripted test
# bundle system is wired up. A successful entry here in the test log proves the
# game booted into 1936 without an early CTD and that bundles are being parsed.

last_date = "1936.2.1"

tests = {
	scenario_boots = {
		success = {
			date > "1936.1.3"
		}
		fail = {
			always = no
		}
		run_count = 1
	}
}
